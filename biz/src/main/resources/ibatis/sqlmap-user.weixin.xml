<?xml version="1.0" encoding="GB2312"?>
<!DOCTYPE sqlMap
	PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
	"http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="user.weixin">
	
	<typeAlias alias="user" type="com.hsh24.mall.api.user.bo.User" />
	
	<select id="getUser" parameterClass="user" resultClass="user">
	<![CDATA[
		SELECT t.id,
		  t.user_id AS userId,
		  t.open_id AS openId
		FROM mall.mall_tb_user_weixin t
		WHERE t.state  = 'U'
	]]>
		<dynamic>
			<isNotEmpty property="openId" prepend="and">
			<![CDATA[ t.open_id = #openId# ]]>
			</isNotEmpty>
			<isNotEmpty property="userId" prepend="and">
			<![CDATA[ t.user_id = #userId# ]]>
			</isNotEmpty>
		</dynamic>
	</select>
	
	<insert id="createUser" parameterClass="user">
	<![CDATA[
		INSERT
		INTO mall.mall_tb_user_weixin
		  (
			USER_ID,
			OPEN_ID,
			STATE,
			CREATE_DATE,
			CREATE_USER,
			MODIFY_DATE,
			MODIFY_USER
		  )
		  VALUES
		  (
			#userId:VARCHAR#,
			#openId:VARCHAR#,
			'U',
			NOW(),
			#modifyUser:VARCHAR#,
			NOW(),
			#modifyUser:VARCHAR#
		  )
	]]>
		<selectKey resultClass="java.lang.Long" keyProperty="id">
		<![CDATA[
			SELECT LAST_INSERT_ID()
		]]>
		</selectKey>
	</insert>
	
</sqlMap>